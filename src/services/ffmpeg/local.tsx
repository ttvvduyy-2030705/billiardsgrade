const streamWebcamToFile = async () => {
  console.log('FFmpeg streamWebcamToFile disabled');
};

const mergeVideos = async () => {
  console.log('FFmpeg mergeVideos disabled');
  return undefined;
};

const cancelStreamWebcamToFile = () => {
  console.log('FFmpeg cancelStreamWebcamToFile disabled');
};

const mergeVideoFiles = async () => {
  console.log('FFmpeg mergeVideoFiles disabled');
  return undefined;
};

const getFiles = async () => {
  console.log('FFmpeg getFiles disabled');
  return [];
};

export {
  streamWebcamToFile,
  mergeVideos,
  cancelStreamWebcamToFile,
  mergeVideoFiles,
  getFiles,
};