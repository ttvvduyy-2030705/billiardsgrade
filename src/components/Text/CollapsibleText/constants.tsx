import { dims } from 'configuration';
import fonts from 'configuration/fonts';

const MIN_DESC_HEIGHT = dims.screenHeight * 0.3;
const SYSTEM_HTML_FONTS = [fonts.Nunito.regular];

export { MIN_DESC_HEIGHT, SYSTEM_HTML_FONTS };
